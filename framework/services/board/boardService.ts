import {TypifiedResponse, Board, BoardCreationResultResponse} from "../../index";

export class BoardService {
    public async createBoard(board: Board): Promise<TypifiedResponse<BoardCreationResultResponse>> {

    }
}